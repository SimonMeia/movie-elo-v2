export type User = {
  createdAt: string
  email: string
  firstName: string
  id: string
  lastName: string
  updatedAt: string
  username: string
}

export type ProfileStatistics = {
  movieCount: number
  timeSpentMin: number
}
